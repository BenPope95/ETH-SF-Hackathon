{
  "name": "blur-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ blur-subgraph",
    "create-local": "graph create --node http://localhost:8020/ blur-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ blur-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 blur-subgraph",
    "test": "graph test"
  },
  "dependencies": { "@graphprotocol/graph-cli": "0.86.0" }
}
