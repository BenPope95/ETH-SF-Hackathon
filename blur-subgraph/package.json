{
  "name": "blur-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ triggers_subgraph",
    "create-local": "graph create --node http://localhost:8020/ triggers_subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ triggers_subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 triggers_subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.65.0",
    "@graphprotocol/graph-ts": "^0.35.1",
    "protobuf-as": "https://github.com/mangas/protobuf-as",
    "tsx": "^4.16.2"
  }
}

